<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UserDAO">
	
	<!-- 생성 -->
	<insert id="createUser" parameterType="user" >
		{call ADD_USER (#{id}, #{password}, #{defaultTableSpace}, #{temporaryTableSpace})}
	</insert>
	
	<!-- 리스트 -->
	<select id="userList" resultType="map">
	SELECT username, LOCK_DATE, DEFAULT_TABLESPACE, TEMPORARY_TABLESPACE 
	FROM DBA_USERS 
	order by username
	</select>
	
	<!-- 삭제 -->
	<delete id="deleteUser" parameterType="user">
		drop user ${id} 
	</delete>

	<!-- 수정 -->
	<update id="updateUser" parameterType="user">
		alter user ${id} 
			  identified by ${password} 
			  default TableSpace ${defaultTableSpace}
			  temporary TableSpace ${temporaryTableSpace} 
			  account ${lock}
	</update>
</mapper>