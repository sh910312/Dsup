<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SearchDAO">
	
	
 	<!-- 단건조회 -->
	<select id="search" parameterType="search" resultType="search">
		select
			search_id,
			contents,
			write_date,
			user_id,
			title,
		from search 
		where search_id = #{searchId}
	</select>
	
	
	
	
	<!-- 키워드 등록  -->
	<insert id="insertSearch" parameterType="search">
		<selectKey keyProperty="searchId" resultType="int" order="BEFORE">
			select
			max(search_id) +1 from search
		</selectKey>
		insert into search(	search_id
						 	,user_id
						 	,title
		        		  	,contents
		)
		
		values( 	 #{searchId}
					,#{userId}
					,#{title}
					,#{contents}
		)

	</insert>
	
	
	
	
	
</mapper>