<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="SearchDAO">
	
	
	
	
	<!-- 단건조회 -->
	<select id="get" parameterType="search" resultType="search">
		select
			search_id,
			contents,
			write_date,
			user_id,
			title,
		from search 
		where search_id = #{search_id}
	</select>
	
	
	<!-- 등록  -->
	<insert id="insertSearch" parameterType="search">
		<selectKey keyProperty="search_id" resultType="int" order="BEFORE">
			select
			max(search_id) +1 from search
		</selectKey>
		insert into search(	search_id
		        		  	,contents
						 	,user_id
						 	,title
		)
		
		values( 	 #{search_id}
					,#{contents}
					,#{user_id}
					,#{title}
		)

	</insert>
	
	
</mapper>